import { index } from '../../taskingForm';

const expect = global.expect;
const describe = global.describe;
const it = global.test;

describe('taskingform index', () => {
  it('should handle index.js', () => {
    expect(index).toEqual(undefined);
  });
});
